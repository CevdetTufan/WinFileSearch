<Page x:Class="WinFileSearch.UI.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:converters="clr-namespace:WinFileSearch.UI.Converters"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Background="{StaticResource PrimaryBackgroundBrush}"
      Title="Settings">

    <Page.Resources>
        <converters:FileSizeConverter x:Key="FileSizeConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <!-- Header -->
            <Grid Margin="0,0,0,24">
                <TextBlock Text="{DynamicResource Settings_Title}" 
                           FontSize="22" 
                           FontWeight="SemiBold"
                           Foreground="{StaticResource PrimaryTextBrush}"/>
            </Grid>

            <!-- General Section -->
            <TextBlock Text="{DynamicResource Settings_General}" 
                       FontSize="11" 
                       FontWeight="SemiBold"
                       Foreground="{StaticResource SecondaryTextBrush}"
                       Margin="0,0,0,12"/>

            <!-- Background Indexing Toggle -->
            <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{DynamicResource Settings_BackgroundIndexing}" 
                                   FontSize="14" 
                                   FontWeight="Medium"
                                   Foreground="{StaticResource PrimaryTextBrush}"/>
                        <TextBlock Text="{DynamicResource Settings_BackgroundIndexingDesc}" 
                                   FontSize="12"
                                   Foreground="{StaticResource SecondaryTextBrush}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                    <CheckBox Grid.Column="1" 
                              Style="{StaticResource ToggleSwitchStyle}"
                              IsChecked="{Binding BackgroundIndexingEnabled}"
                              VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Start with Windows Toggle -->
            <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{DynamicResource Settings_StartWithWindows}" 
                                   FontSize="14" 
                                   FontWeight="Medium"
                                   Foreground="{StaticResource PrimaryTextBrush}"/>
                        <TextBlock Text="{DynamicResource Settings_StartWithWindowsDesc}" 
                                   FontSize="12"
                                   Foreground="{StaticResource SecondaryTextBrush}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                    <CheckBox Grid.Column="1" 
                              Style="{StaticResource ToggleSwitchStyle}"
                              IsChecked="{Binding StartWithWindows}"
                              VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Minimize to Tray Toggle -->
            <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{DynamicResource Settings_MinimizeToTray}" 
                                   FontSize="14" 
                                   FontWeight="Medium"
                                   Foreground="{StaticResource PrimaryTextBrush}"/>
                        <TextBlock Text="{DynamicResource Settings_MinimizeToTrayDesc}" 
                                   FontSize="12"
                                   Foreground="{StaticResource SecondaryTextBrush}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                    <CheckBox Grid.Column="1" 
                              Style="{StaticResource ToggleSwitchStyle}"
                              IsChecked="{Binding MinimizeToTray}"
                              VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Language Selection -->
            <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{DynamicResource Settings_Language}" 
                                   FontSize="14" 
                                   FontWeight="Medium"
                                   Foreground="{StaticResource PrimaryTextBrush}"/>
                        <TextBlock Text="{DynamicResource Settings_LanguageDesc}" 
                                   FontSize="12"
                                   Foreground="{StaticResource SecondaryTextBrush}"
                                   Margin="0,4,0,0"/>
                    </StackPanel>
                    <ComboBox Grid.Column="1"
                              ItemsSource="{Binding AvailableLanguages}"
                              SelectedItem="{Binding SelectedLanguageOption}"
                              Width="140"
                              VerticalAlignment="Center">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" Foreground="{StaticResource PrimaryTextBrush}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </Grid>
            </Border>

            <!-- Rebuild Index -->
            <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="{DynamicResource Settings_RebuildIndex}" 
                               FontSize="14" 
                               FontWeight="Medium"
                               Foreground="{StaticResource PrimaryTextBrush}"/>
                    <TextBlock Text="{DynamicResource Settings_RebuildIndexDesc}" 
                               FontSize="12"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               Margin="0,4,0,12"/>
                    
                    <!-- Progress Bar (visible during indexing) -->
                    <ProgressBar Style="{StaticResource IndexingProgressBarStyle}"
                                 Value="{Binding IndexingProgress}"
                                 Maximum="100"
                                 Margin="0,0,0,8"
                                 Visibility="{Binding IsIndexing, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    
                    <TextBlock Text="{Binding IndexingStatus}"
                               FontSize="12"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               Margin="0,0,0,12"
                               Visibility="{Binding IsIndexing, Converter={StaticResource BoolToVisibilityConverter}}"/>

                    <Button Command="{Binding RebuildIndexCommand}">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
                                <Setter Property="Content" Value="{DynamicResource Settings_RebuildNow}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsIndexing}" Value="True">
                                        <Setter Property="Content" Value="{DynamicResource Settings_CancelRebuild}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Included Folders Section -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="{DynamicResource Settings_IncludedFolders}" 
                           FontSize="11" 
                           FontWeight="SemiBold"
                           Foreground="{StaticResource SecondaryTextBrush}"
                           VerticalAlignment="Center"/>
                <Button Grid.Column="1"
                        Content="{DynamicResource Settings_AddFolder}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding AddFolderCommand}"/>
            </Grid>

            <!-- Drag & Drop Zone -->
            <Border x:Name="DropZone"
                    Style="{StaticResource CardPanelStyle}"
                    BorderThickness="2"
                    BorderBrush="{StaticResource SecondaryBackgroundBrush}"
                    Margin="0,0,0,12"
                    MinHeight="100"
                    AllowDrop="True"
                    DragEnter="DropZone_DragEnter"
                    DragLeave="DropZone_DragLeave"
                    Drop="DropZone_Drop">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20">
                    <Path Data="M10,4H4C2.89,4 2,4.89 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V8C22,6.89 21.1,6 20,6H12L10,4Z"
                          Fill="{StaticResource SecondaryTextBrush}"
                          Width="32" Height="32"
                          Stretch="Uniform"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,12"/>
                    <TextBlock Text="{DynamicResource Settings_DragDropHint}"
                               FontSize="14"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{DynamicResource Settings_DragDropSubHint}"
                               FontSize="12"
                               Foreground="{StaticResource SecondaryTextBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,4,0,0"
                               Opacity="0.7"/>
                </StackPanel>
            </Border>

            <ItemsControl ItemsSource="{Binding IncludedFolders}" Margin="0,0,0,24">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Folder Icon -->
                                <Path Grid.Column="0"
                                      Data="M10,4H4C2.89,4 2,4.89 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V8C22,6.89 21.1,6 20,6H12L10,4Z"
                                      Fill="{StaticResource FolderIconBrush}"
                                      Width="24" Height="24"
                                      Stretch="Uniform"
                                      Margin="0,0,12,0"/>

                                <!-- Folder Info -->
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Path}"
                                               FontSize="14"
                                               Foreground="{StaticResource PrimaryTextBrush}"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock FontSize="12" Foreground="{StaticResource SecondaryTextBrush}">
                                            <Run Text="{Binding FileCount, StringFormat='{}{0:N0} files'}"/>
                                            <Run Text=" • "/>
                                            <Run Text="{Binding TotalSize, Converter={StaticResource FileSizeConverter}}"/>
                                        </TextBlock>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Remove Button -->
                                <Button Grid.Column="2"
                                        Content="⊖"
                                        FontSize="18"
                                        Background="Transparent"
                                        Foreground="{StaticResource SecondaryTextBrush}"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Command="{Binding DataContext.RemoveFolderCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Excluded Folders Section -->
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="{DynamicResource Settings_ExcludedFolders}" 
                           FontSize="11" 
                           FontWeight="SemiBold"
                           Foreground="{StaticResource SecondaryTextBrush}"
                           VerticalAlignment="Center"/>
                <Button Grid.Column="1"
                        Content="{DynamicResource Settings_AddExclusion}"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding AddExclusionCommand}"/>
            </Grid>

            <ItemsControl ItemsSource="{Binding ExcludedFolders}" Margin="0,0,0,24">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Excluded Folder Icon -->
                                <Path Grid.Column="0"
                                      Data="M10,4H4C2.89,4 2,4.89 2,6V18A2,2 0 0,0 4,20H20A2,2 0 0,0 22,18V8C22,6.89 21.1,6 20,6H12L10,4Z"
                                      Fill="{StaticResource SecondaryTextBrush}"
                                      Width="24" Height="24"
                                      Stretch="Uniform"
                                      Margin="0,0,12,0"
                                      Opacity="0.5"/>

                                <!-- Folder Info -->
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Path}"
                                               FontSize="14"
                                               Foreground="{StaticResource PrimaryTextBrush}"/>
                                    <TextBlock Text="{DynamicResource Common_ExcludedFromIndexing}"
                                               FontSize="12"
                                               Foreground="{StaticResource SecondaryTextBrush}"
                                               Margin="0,4,0,0"/>
                                </StackPanel>

                                <!-- Remove Button -->
                                <Button Grid.Column="2"
                                        Content="⊖"
                                        FontSize="18"
                                        Background="Transparent"
                                        Foreground="{StaticResource SecondaryTextBrush}"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Command="{Binding DataContext.RemoveExclusionCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Stats -->
            <StackPanel HorizontalAlignment="Center" Margin="0,20,0,0">
                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Text="{DynamicResource Common_LastIndexed}"
                               FontSize="12"
                               Foreground="{StaticResource SecondaryTextBrush}"/>
                    <TextBlock Text=" "
                               FontSize="12"
                               Foreground="{StaticResource SecondaryTextBrush}"/>
                    <TextBlock Text="{Binding LastIndexed, StringFormat='MMM d, yyyy h:mm tt', TargetNullValue={StaticResource Common_Never}}"
                               FontSize="12"
                               Foreground="{StaticResource SecondaryTextBrush}"/>
                </StackPanel>
                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Margin="0,4,0,0">
                    <TextBlock Text="{DynamicResource Common_TotalFiles}"
                               FontSize="12"
                               Foreground="{StaticResource SecondaryTextBrush}"/>
                    <TextBlock Text=" "
                               FontSize="12"
                               Foreground="{StaticResource SecondaryTextBrush}"/>
                    <TextBlock Text="{Binding TotalFilesIndexed, StringFormat='{}{0:N0}'}"
                               FontSize="12"
                               Foreground="{StaticResource SecondaryTextBrush}"/>
                </StackPanel>
            </StackPanel>

            <!-- Performance Metrics Section -->
            <TextBlock Text="{DynamicResource Perf_Title}" 
                       FontSize="11" 
                       FontWeight="SemiBold"
                       Foreground="{StaticResource SecondaryTextBrush}"
                       Margin="0,24,0,12"/>

            <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,0,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Total Searches -->
                    <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,16">
                        <TextBlock Text="{DynamicResource Perf_TotalSearches}" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}"/>
                        <TextBlock Text="{Binding TotalSearches}" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}"/>
                    </StackPanel>

                    <!-- Avg Search Time -->
                    <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,0,16">
                        <TextBlock Text="{DynamicResource Perf_AvgSearchTime}" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}"/>
                        <TextBlock FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}">
                            <Run Text="{Binding AverageSearchTimeMs}"/><Run Text=" ms"/>
                        </TextBlock>
                    </StackPanel>

                    <!-- Memory Usage -->
                    <StackPanel Grid.Column="0" Grid.Row="1">
                        <TextBlock Text="{DynamicResource Perf_MemoryUsage}" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}"/>
                        <TextBlock FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}">
                            <Run Text="{Binding MemoryUsageMB}"/><Run Text=" MB"/>
                        </TextBlock>
                    </StackPanel>

                    <!-- Uptime -->
                    <StackPanel Grid.Column="1" Grid.Row="1">
                        <TextBlock Text="{DynamicResource Perf_Uptime}" FontSize="12" Foreground="{StaticResource SecondaryTextBrush}"/>
                        <TextBlock Text="{Binding Uptime}" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource PrimaryTextBrush}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                <Button Content="{DynamicResource Perf_Refresh}" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding RefreshMetricsCommand}"
                        Margin="0,0,12,0"/>
                <Button Content="{DynamicResource Perf_OpenLogs}" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding OpenLogFolderCommand}"/>
            </StackPanel>

            <!-- About & Updates Section -->
            <TextBlock Text="{DynamicResource About_Title}" 
                       FontSize="11" 
                       FontWeight="SemiBold"
                       Foreground="{StaticResource SecondaryTextBrush}"
                       Margin="0,24,0,12"/>

            <Border Style="{StaticResource CardPanelStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <!-- Current Version -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="WinFileSearch" 
                                       FontSize="16" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource PrimaryTextBrush}"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock Text="{DynamicResource About_CurrentVersion}"
                                           FontSize="12"
                                           Foreground="{StaticResource SecondaryTextBrush}"/>
                                <TextBlock Text=" "
                                           FontSize="12"
                                           Foreground="{StaticResource SecondaryTextBrush}"/>
                                <TextBlock Text="{Binding CurrentVersion}"
                                           FontSize="12"
                                           Foreground="{StaticResource SecondaryTextBrush}"/>
                            </StackPanel>
                        </StackPanel>
                        <Button Grid.Column="1"
                                Content="{DynamicResource About_CheckUpdates}"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding CheckForUpdatesCommand}"/>
                    </Grid>

                    <!-- Update Available Banner -->
                    <Border Background="#1E4CAF50" 
                            CornerRadius="8" 
                            Padding="16"
                            Visibility="{Binding IsUpdateAvailable, Converter={StaticResource BoolToVisibilityConverter}}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Update Info -->
                            <StackPanel Grid.Row="0" Margin="0,0,0,12" Orientation="Horizontal">
                                <TextBlock Text="🎉 " FontSize="14" FontWeight="SemiBold" Foreground="#4CAF50"/>
                                <TextBlock Text="{DynamicResource About_NewVersion}" FontSize="14" FontWeight="SemiBold" Foreground="#4CAF50"/>
                                <TextBlock Text=" " FontSize="14" FontWeight="SemiBold" Foreground="#4CAF50"/>
                                <TextBlock Text="{Binding LatestVersion}" FontSize="14" FontWeight="SemiBold" Foreground="#4CAF50"/>
                            </StackPanel>

                            <!-- Release Notes Preview -->
                            <TextBlock Grid.Row="1" 
                                       Text="{Binding UpdateReleaseNotes}" 
                                       TextWrapping="Wrap"
                                       MaxHeight="80"
                                       FontSize="12"
                                       Foreground="{StaticResource SecondaryTextBrush}"
                                       Margin="0,0,0,12"/>

                            <!-- Download Progress (shown when downloading) -->
                            <StackPanel Grid.Row="2" 
                                        Visibility="{Binding IsDownloadingUpdate, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="{DynamicResource About_Downloading}" 
                                           FontSize="12" 
                                           Foreground="{StaticResource SecondaryTextBrush}"
                                           Margin="0,0,0,8"/>
                                <ProgressBar Value="{Binding DownloadProgress}" 
                                             Minimum="0" 
                                             Maximum="100" 
                                             Height="6"
                                             Foreground="{StaticResource AccentBrush}"/>
                            </StackPanel>

                            <!-- Action Buttons (hidden when downloading) -->
                            <StackPanel Grid.Row="2" 
                                        Orientation="Horizontal" 
                                        HorizontalAlignment="Right">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsDownloadingUpdate}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <Button Content="{DynamicResource About_ViewGitHub}"
                                        Style="{StaticResource SecondaryButtonStyle}"
                                        Command="{Binding OpenReleasePageCommand}"
                                        Margin="0,0,12,0"/>
                                <Button Content="{DynamicResource About_DownloadInstall}"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Command="{Binding DownloadAndInstallUpdateCommand}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
